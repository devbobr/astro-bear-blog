---
type Props = {
	class?: string;
	[key: string]: any;
};

const { class: className, ...rest } = Astro.props;
---

<div
	class:list={["grid grid-flow-row md:grid-flow-col auto-cols-auto", className]}
	{...rest}
>
	<slot />
</div>
